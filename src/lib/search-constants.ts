/**
 * Shared search validation constants.
 * Used by both the API route and the client SearchInput component
 * to ensure consistent behaviour and prevent silent drift.
 */
export const SEARCH_MIN_LENGTH = 2;
export const SEARCH_MAX_LENGTH = 100;
