---
phase: 04-discovery-layer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/browse/page.tsx
  - src/components/category-filter.tsx
  - src/components/sort-dropdown.tsx
  - src/components/cask-grid.tsx
autonomous: true
requirements:
  - BRWS-02
  - BRWS-03

must_haves:
  truths:
    - User can click a category pill to filter the browse grid to that category
    - User can change the sort order and see results re-ordered immediately
    - Filter and sort state persist in URL for back-button and sharing
    - Responsive grid adapts from 1 to 4 columns based on viewport width
  artifacts:
    - path: "src/components/category-filter.tsx"
      provides: "Client island for category filter pills with URL state management"
      min_lines: 50
    - path: "src/components/sort-dropdown.tsx"
      provides: "Client island for sort dropdown with URL state management"
      min_lines: 40
    - path: "src/app/browse/page.tsx"
      provides: "Browse page with category/sort param handling"
      contains: "category?: string; sort?: string"
    - path: "src/components/cask-grid.tsx"
      provides: "Responsive grid with 1/2/3/4 column breakpoints"
      contains: "lg:grid-cols-3 xl:grid-cols-4"
  key_links:
    - from: "src/components/category-filter.tsx"
      to: "src/app/browse/page.tsx"
      via: "URL query param ?category=X"
      pattern: "params.set\\('category'"
    - from: "src/components/sort-dropdown.tsx"
      to: "src/app/browse/page.tsx"
      via: "URL query param ?sort=Y"
      pattern: "params.set\\('sort'"
    - from: "src/app/browse/page.tsx"
      to: "src/lib/queries.ts"
      via: "getCasksPageFiltered call"
      pattern: "getCasksPageFiltered"
---

<objective>
Add category filtering and sort controls to the browse page with URL state persistence.

Purpose: Users can explore casks by category and re-order results by popularity, alphabetical, or recently updated. This is the core discovery enhancement — after this task, users have a complete App Store-like browsing experience.

Output: Category filter pill bar, sort dropdown, extended browse page with filter/sort logic, responsive 4-column grid.
</objective>

<execution_context>
@/Users/yash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-discovery-layer/04-CONTEXT.md
@.planning/phases/04-discovery-layer/04-RESEARCH.md
@.planning/phases/04-discovery-layer/04-PATTERNS.md
@.planning/phases/04-discovery-layer/04-UI-SPEC.md
@.planning/phases/02-catalog-ui/02-02-SUMMARY.md
@.planning/phases/03-search-security/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryFilter and SortDropdown client islands</name>
  <files>src/components/category-filter.tsx, src/components/sort-dropdown.tsx</files>
  <read_first>
    - src/components/search-input.tsx (URL state management pattern with useSearchParams + useRouter)
    - src/components/cask-card.tsx (pill badge styling for filter pills)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 210-320 for CategoryFilter pattern, lines 324-394 for SortDropdown pattern)
    - .planning/phases/04-discovery-layer/04-UI-SPEC.md (lines 110-138 for copy, lines 171-188 for interaction patterns)
  </read_first>
  <action>
Create `src/components/category-filter.tsx` as a client component:
- `'use client'` directive at top
- Import `useSearchParams, useRouter, usePathname` from 'next/navigation'
- Props: `{ currentCategory?: string; categories: { category: string | null }[] }`
- Function `setCategory(cat: string | null)`: creates URLSearchParams from current searchParams, sets/deletes 'category' param, deletes 'page' param (reset to page 1), calls `router.replace(pathname + '?' + params.toString(), { scroll: false })`
- Renders pill bar: `<div role="group" aria-label="Category filter" style={{ display: 'flex', gap: '8px', flexWrap: 'wrap' }}>`
- First button: "All Apps" (cat=null), active when currentCategory is undefined/null
- Map over categories: render button for each with `onClick={() => setCategory(c.category)}`, `aria-pressed={isActive(c.category)}`
- Pill styling (from PATTERNS.md lines 275-292): background var(--color-surface), border var(--color-border), borderRadius 9999px, padding 6px 16px, fontSize 0.9375rem, fontWeight 500
- Active pill styling: background var(--color-primary-dim), borderColor var(--color-primary), color var(--color-primary-hover)
- Transition: all 0.15s ease

Create `src/components/sort-dropdown.tsx` as a client component:
- `'use client'` directive at top
- Import `useSearchParams, useRouter, usePathname` from 'next/navigation'
- Const `SORT_OPTIONS = { popular: 'Popular', alphabetical: 'A-Z', updated: 'Recently Updated' } as const`
- Props: `{ currentSort: string }`
- Function `handleChange(e: React.ChangeEvent<HTMLSelectElement>)`: creates URLSearchParams, sets 'sort' param, deletes 'page', calls router.replace
- Renders native `<select>` styled to match filter pills: background var(--color-surface), border var(--color-border), borderRadius 10px, padding 6px 12px, fontSize 0.9375rem, fontWeight 500, color var(--color-text)
- aria-label="Sort casks by"
- Maps over SORT_OPTIONS: `<option key={key} value={key}>{label}</option>`
  </action>
  <verify>
    <automated>grep -c "'use client'" src/components/category-filter.tsx</automated>
    <automated>grep -c "useSearchParams" src/components/category-filter.tsx</automated>
    <automated>grep -c "router.replace" src/components/category-filter.tsx</automated>
    <automated>grep -c "All Apps" src/components/category-filter.tsx</automated>
    <automated>grep -c "'use client'" src/components/sort-dropdown.tsx</automated>
    <automated>grep -c "SORT_OPTIONS" src/components/sort-dropdown.tsx</automated>
    <automated>grep -c "handleChange" src/components/sort-dropdown.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/category-filter.tsx exists with 'use client' directive
    - CategoryFilter component uses useSearchParams, useRouter, usePathname for URL state
    - setCategory function updates 'category' param and deletes 'page' param
    - Pill bar has role="group" and aria-label="Category filter"
    - "All Apps" button present for showing all categories (cat=null)
    - Active pill styling uses var(--color-primary-dim) background and var(--color-primary) border
    - src/components/sort-dropdown.tsx exists with 'use client' directive
    - SORT_OPTIONS const defines popular/alphabetical/updated with display labels
    - Native select element styled to match filter pill aesthetic
    - handleChange updates 'sort' param and deletes 'page' param
    - Both components call router.replace with { scroll: false }
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
Two client islands created: CategoryFilter (pill bar with URL state management) and SortDropdown (native select with URL state management). Both reset pagination on change and preserve existing URL params.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire filters into browse page and update grid layout</name>
  <files>src/app/browse/page.tsx, src/components/cask-grid.tsx</files>
  <read_first>
    - src/app/browse/page.tsx (current searchParams handling for page and q)
    - src/components/cask-grid.tsx (current 2-column grid)
    - src/lib/queries.ts (getCasksPageFiltered, getCasksCountFiltered, getCategories signatures)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 734-829 for browse page extension pattern)
    - .planning/phases/04-discovery-layer/04-RESEARCH.md (lines 165-224 for searchParams Promise pattern)
  </read_first>
  <action>
Update `src/app/browse/page.tsx`:
- Extend searchParams type (line 13): `searchParams: Promise<{ page?: string; q?: string; category?: string; sort?: string }>`
- Destructure category and sort (line 15): `const { page: pageParam, q, category, sort } = await searchParams;`
- Add imports: `getCasksPageFiltered, getCasksCountFiltered, getCategories` from '@/lib/queries', `CategoryFilter` from '@/components/category-filter', `SortDropdown` from '@/components/sort-dropdown'
- In normal browse path (after line 32, before page calculation):
  - Validate sort: `const sortKey = (sort === 'alphabetical' || sort === 'updated') ? sort : 'popular';`
  - Replace `getCasksPage(page)` with `getCasksPageFiltered({ category, sort: sortKey, page })`
  - Replace `getCasksCount()` with `getCasksCountFiltered(category)`
  - Add `getCategories()` to Promise.all array
- Before `<CaskGrid>` (line 44), add filter/sort controls:
  ```tsx
  <div style={{ display: 'flex', gap: '16px', marginBottom: '24px', flexWrap: 'wrap', alignItems: 'center' }}>
    <CategoryFilter currentCategory={category} categories={categories} />
    <SortDropdown currentSort={sortKey} />
  </div>
  ```

Update `src/components/cask-grid.tsx`:
- Change line 31 from `className="grid grid-cols-1 md:grid-cols-2 gap-3"` to `className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6"`
- This adds 3-column breakpoint at 1024px (lg) and 4-column at 1440px (xl), increases gap from 12px to 24px per D-15
  </action>
  <verify>
    <automated>grep -c "category?: string; sort?: string" src/app/browse/page.tsx</automated>
    <automated>grep -c "getCasksPageFiltered" src/app/browse/page.tsx</automated>
    <automated>grep -c "getCasksCountFiltered" src/app/browse/page.tsx</automated>
    <automated>grep -c "getCategories" src/app/browse/page.tsx</automated>
    <automated>grep -c "CategoryFilter" src/app/browse/page.tsx</automated>
    <automated>grep -c "SortDropdown" src/app/browse/page.tsx</automated>
    <automated>grep -c "lg:grid-cols-3 xl:grid-cols-4" src/components/cask-grid.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/browse/page.tsx searchParams type includes category and sort
    - Browse page destructures category and sort from await searchParams
    - sortKey validation: defaults to 'popular' if not 'alphabetical' or 'updated'
    - Browse page calls getCasksPageFiltered with { category, sort: sortKey, page }
    - Browse page calls getCasksCountFiltered(category) for filtered pagination
    - Browse page calls getCategories() to fetch category list
    - CategoryFilter and SortDropdown components imported and rendered above CaskGrid
    - Filter/sort controls wrapped in flex container with gap-16px, marginBottom-24px
    - src/components/cask-grid.tsx uses 4-breakpoint grid: 1/2/3/4 columns at sm/lg/xl
    - Grid gap increased to 24px (gap-6)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
Browse page now reads category and sort from URL, validates sort param, calls filtered query functions, and renders CategoryFilter + SortDropdown above the grid. CaskGrid layout updated to 4-column responsive grid (1/2/3/4 at mobile/tablet/desktop/wide). Full filtering and sorting experience is live.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browse page | Untrusted category/sort params from URL query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | src/app/browse/page.tsx | mitigate | Sort param validated against whitelist ['popular', 'alphabetical', 'updated'] with fallback to 'popular' (line ~36) |
| T-04-05 | Tampering | src/app/browse/page.tsx | mitigate | Category param passed to parameterized Drizzle query (getCasksPageFiltered) — prevents SQL injection |
| T-04-06 | Denial of Service | src/components/category-filter.tsx | accept | Rapid filter clicks trigger router.replace but no backend load (ISR-cached queries) |
</threat_model>

<verification>
## Filter UI Rendered
```bash
grep -E "(CategoryFilter|SortDropdown)" src/app/browse/page.tsx
```
Expected: Both component imports and JSX render calls present.

## Grid Responsive Classes
```bash
grep "lg:grid-cols-3 xl:grid-cols-4" src/components/cask-grid.tsx
```
Expected: Found in className string.

## TypeScript Clean
```bash
npx tsc --noEmit
```
Expected: Exit 0, no type errors.

## Build Success
```bash
npm run build
```
Expected: Compiles with no errors; browse page remains dynamic (ƒ).
</verification>

<success_criteria>
- [x] CategoryFilter client island updates URL ?category param and resets ?page
- [x] SortDropdown client island updates URL ?sort param and resets ?page
- [x] Browse page reads category and sort from searchParams Promise
- [x] Browse page validates sort param with whitelist fallback
- [x] Browse page calls getCasksPageFiltered with category/sort/page
- [x] Browse page calls getCasksCountFiltered for accurate filtered pagination
- [x] Browse page calls getCategories and passes to CategoryFilter for pill population
- [x] CaskGrid uses responsive 4-breakpoint layout (1/2/3/4 columns)
- [x] TypeScript compiles with no errors
- [x] Next.js build succeeds
</success_criteria>

<output>
Create `.planning/phases/04-discovery-layer/04-02-SUMMARY.md` when done
</output>
