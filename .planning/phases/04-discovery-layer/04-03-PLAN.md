---
phase: 04-discovery-layer
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/github-stats-card.tsx
  - src/components/star-badge.tsx
  - src/components/cask-card.tsx
  - src/app/cask/[token]/page.tsx
autonomous: true
requirements:
  - DETL-05

must_haves:
  truths:
    - Cask detail pages with GitHub repos show star/fork/issue counts
    - Browse cards show star count badge when GitHub data available
    - GitHub stats only render when github_enriched is true
  artifacts:
    - path: "src/components/github-stats-card.tsx"
      provides: "Server component for detail page GitHub stats display"
      min_lines: 70
    - path: "src/components/star-badge.tsx"
      provides: "Pure component for star count pill on browse cards"
      min_lines: 15
    - path: "src/components/cask-card.tsx"
      provides: "CaskCard with conditional star badge in metadata strip"
      contains: "github_enriched"
    - path: "src/app/cask/[token]/page.tsx"
      provides: "Detail page with GitHub stats card section"
      contains: "GitHubStatsCard"
  key_links:
    - from: "src/components/github-stats-card.tsx"
      to: "src/db/schema.ts"
      via: "CaskSelectRow type"
      pattern: "github_stars.*github_forks.*github_issues"
    - from: "src/components/cask-card.tsx"
      to: "src/components/star-badge.tsx"
      via: "conditional render"
      pattern: "github_enriched.*StarBadge"
---

<objective>
Add GitHub social proof metrics to detail pages and browse cards.

Purpose: Users can see repository health signals (stars, forks, open issues) for casks with GitHub upstreams. This completes the discovery layer by adding credibility signals that help users evaluate apps.

Output: GitHubStatsCard component for detail pages, StarBadge component for browse cards, conditional rendering based on github_enriched flag.
</objective>

<execution_context>
@/Users/yash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-discovery-layer/04-CONTEXT.md
@.planning/phases/04-discovery-layer/04-RESEARCH.md
@.planning/phases/04-discovery-layer/04-PATTERNS.md
@.planning/phases/04-discovery-layer/04-UI-SPEC.md
@.planning/phases/02-catalog-ui/02-03-SUMMARY.md
@.claude/skills/sketch-findings-brewindex/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHubStatsCard and StarBadge components</name>
  <files>src/components/github-stats-card.tsx, src/components/star-badge.tsx</files>
  <read_first>
    - src/app/cask/[token]/page.tsx (install stats section pattern, lines 220-350 per PATTERNS.md reference)
    - src/components/cask-card.tsx (installs pill pattern for star badge styling)
    - src/lib/format.ts (formatInstallCount utility)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 398-646 for GitHubStatsCard pattern, lines 599-646 for StarBadge pattern)
    - .planning/phases/04-discovery-layer/04-UI-SPEC.md (lines 124-131 for copy, lines 189-198 for interaction patterns)
  </read_first>
  <action>
Create `src/components/github-stats-card.tsx` as a server component (no 'use client'):
- Import: `CaskSelectRow` from '@/db/schema', `formatInstallCount` from '@/lib/format', `Star, GitFork, AlertCircle` from 'lucide-react'
- Props: `{ cask: CaskSelectRow }`
- Early return `null` if `!cask.github_enriched || cask.github_stars === null` (per D-12)
- Section heading: "REPOSITORY STATS" at fontSize 0.6875rem, fontWeight 400, textTransform uppercase, letterSpacing 0.07em, color var(--color-text-faint), marginBottom 10px
- Stats card container: background var(--color-surface), border 1px solid var(--color-border), borderRadius 10px, padding 16px 20px, display flex, alignItems center, gap 24px, flexWrap wrap
- Three stats with separators (from PATTERNS.md lines 499-590):
  1. **Stars**: Star icon (size 20, color #9581ff), count formatted with formatInstallCount(cask.github_stars), label "stars" (fontSize 0.6875rem, color var(--color-text-faint))
  2. Separator: width 1px, height 32px, background var(--color-border-subtle)
  3. **Forks**: GitFork icon (size 16, color var(--color-text-muted)), formatInstallCount(cask.github_forks), label "forks"
  4. Separator (same as above)
  5. **Open Issues**: AlertCircle icon (size 16, color var(--color-text-muted)), formatInstallCount(cask.github_issues), label "open issues"
- Each stat: icon + flex column (count at 1rem fontWeight 700 + label at 0.6875rem)

Create `src/components/star-badge.tsx` as a pure component (no imports except formatInstallCount):
- Import `formatInstallCount` from '@/lib/format'
- Props: `{ count: number }`
- Returns single `<span>` with pill styling (from PATTERNS.md lines 626-644):
  - background: rgba(124,106,255,0.15)
  - border: 1px solid rgba(124,106,255,0.25)
  - color: #9581ff
  - borderRadius: 9999px
  - padding: 2px 8px
  - fontSize: 0.6875rem
  - whiteSpace: nowrap
- Content: `★ {formatInstallCount(count)}`
  </action>
  <verify>
    <automated>grep -c "formatInstallCount" src/components/github-stats-card.tsx</automated>
    <automated>grep -c "github_enriched" src/components/github-stats-card.tsx</automated>
    <automated>grep -c "Star.*GitFork.*AlertCircle" src/components/github-stats-card.tsx</automated>
    <automated>grep -c "REPOSITORY STATS" src/components/github-stats-card.tsx</automated>
    <automated>grep -c "formatInstallCount" src/components/star-badge.tsx</automated>
    <automated>grep -c "★" src/components/star-badge.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/github-stats-card.tsx exists as server component (no 'use client')
    - GitHubStatsCard returns null if !github_enriched or github_stars is null
    - Section heading "REPOSITORY STATS" styled with uppercase, letter-spacing, faint color
    - Three stats (stars/forks/issues) with lucide icons, formatted counts, and labels
    - Separator divs (1px width, 32px height) between each stat
    - Star icon size 20px with primary accent color (#9581ff), others size 16px muted
    - src/components/star-badge.tsx exists as pure component
    - StarBadge renders pill with "★ {count}" using formatInstallCount
    - Pill styling matches install count pill (rgba(124,106,255,0.15) background)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
Two components created: GitHubStatsCard (detail page horizontal stat row with icons) and StarBadge (compact star count pill for browse cards). Both use formatInstallCount for number formatting and conditionally render based on github_enriched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GitHub stats into detail page and browse cards</name>
  <files>src/app/cask/[token]/page.tsx, src/components/cask-card.tsx</files>
  <read_first>
    - src/app/cask/[token]/page.tsx (current layout to find insertion point after install command section)
    - src/components/cask-card.tsx (metadata strip structure)
    - src/components/github-stats-card.tsx (component signature)
    - src/components/star-badge.tsx (component signature)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 650-731 for cask-card modification)
  </read_first>
  <action>
Update `src/app/cask/[token]/page.tsx`:
- Add import: `GitHubStatsCard` from '@/components/github-stats-card'
- Find the install command section (search for "Install Command" or the copy button section)
- After the install command section and before the stats/metadata grid, insert:
  ```tsx
  {/* GitHub Stats */}
  <GitHubStatsCard cask={c} />
  ```
- Component will self-conditionally render (returns null if no GitHub data)

Update `src/components/cask-card.tsx`:
- Add import: `StarBadge` from '@/components/star-badge'
- In metadata strip (around line 88-141), after the installs pill and before the platform pill, insert:
  ```tsx
  {/* GitHub stars badge */}
  {cask.github_enriched && cask.github_stars !== null && (
    <StarBadge count={cask.github_stars} />
  )}
  ```
- Conditional: only render if `github_enriched` is true AND `github_stars` is not null (per D-12, PATTERNS.md line 714-716)
  </action>
  <verify>
    <automated>grep -c "GitHubStatsCard" src/app/cask/[token]/page.tsx</automated>
    <automated>grep -c "<GitHubStatsCard cask=" src/app/cask/[token]/page.tsx</automated>
    <automated>grep -c "StarBadge" src/components/cask-card.tsx</automated>
    <automated>grep -c "github_enriched.*StarBadge" src/components/cask-card.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/cask/[token]/page.tsx imports GitHubStatsCard
    - Detail page renders `<GitHubStatsCard cask={c} />` after install command section
    - src/components/cask-card.tsx imports StarBadge
    - CaskCard renders StarBadge conditionally: only if github_enriched && github_stars !== null
    - StarBadge positioned in metadata strip after installs pill, before platform pill
    - Conditional check uses both github_enriched and github_stars !== null (avoids showing 0 stars for non-GitHub casks)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
Detail page displays GitHub stats card (stars/forks/issues) below install command when GitHub data available. Browse cards show star count badge in metadata strip when GitHub enriched. Both use formatInstallCount for consistent number formatting (12.3k).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database→display | GitHub data from database rendered in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | src/components/github-stats-card.tsx | accept | GitHub stats displayed as-is from database; no user input or external fetch at render time |
| T-04-08 | Information Disclosure | src/components/star-badge.tsx | accept | GitHub star counts are public information; displaying them adds value without leaking private data |
</threat_model>

<verification>
## GitHub Stats Components Exist
```bash
ls -la src/components/github-stats-card.tsx src/components/star-badge.tsx
```
Expected: Both files present.

## Detail Page Integration
```bash
grep "GitHubStatsCard" src/app/cask/[token]/page.tsx
```
Expected: Import and JSX render found.

## Browse Card Integration
```bash
grep -A2 "github_enriched.*StarBadge" src/components/cask-card.tsx
```
Expected: Conditional render with github_enriched && github_stars !== null guard.

## TypeScript Clean
```bash
npx tsc --noEmit
```
Expected: Exit 0, no type errors.
</verification>

<success_criteria>
- [x] GitHubStatsCard component displays stars, forks, open issues with lucide icons
- [x] GitHubStatsCard conditionally renders (null if !github_enriched or github_stars null)
- [x] StarBadge component displays "★ {count}" with primary accent pill styling
- [x] Detail page renders GitHubStatsCard after install command section
- [x] CaskCard renders StarBadge in metadata strip when GitHub data available
- [x] StarBadge only shown for casks with github_enriched = true and github_stars !== null
- [x] Both components use formatInstallCount for consistent number formatting
- [x] TypeScript compiles with no errors
</success_criteria>

<output>
Create `.planning/phases/04-discovery-layer/04-03-SUMMARY.md` when done
</output>
