---
phase: 04-discovery-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations/0002_add_category.sql
  - src/lib/queries.ts
autonomous: true
requirements:
  - BRWS-02
  - BRWS-03

must_haves:
  truths:
    - Database schema has category column for storing categorization data
    - Query layer can filter casks by category and apply dynamic sort order
    - Browse page can receive category and sort params from URL and fetch filtered results
  artifacts:
    - path: "src/db/schema.ts"
      provides: "category column definition"
      contains: "category: text('category')"
    - path: "src/db/migrations/0002_add_category.sql"
      provides: "Migration adding category column and index"
      min_lines: 10
    - path: "src/lib/queries.ts"
      provides: "getCasksPageFiltered, getCasksCountFiltered, getCategories functions"
      exports: ["getCasksPageFiltered", "getCasksCountFiltered", "getCategories"]
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/db/schema.ts"
      via: "category column reference"
      pattern: "casks\\.category"
---

<objective>
Add database and query layer foundation for category filtering and dynamic sorting.

Purpose: Enable browse page to filter by category and sort by popularity/alphabetical/recently-updated. This is the data layer that all subsequent UI features depend on.

Output: Schema migration, extended query functions with category filtering and dynamic sorting, category list query for populating filter UI.
</objective>

<execution_context>
@/Users/yash/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yash/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discovery-layer/04-CONTEXT.md
@.planning/phases/04-discovery-layer/04-RESEARCH.md
@.planning/phases/04-discovery-layer/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category column to schema and create migration</name>
  <files>src/db/schema.ts, src/db/migrations/0002_add_category.sql</files>
  <read_first>
    - src/db/schema.ts (current schema to see column definition pattern)
    - src/db/migrations/0001_add_search_vector.sql (migration file format)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 22-86 for exact patterns)
  </read_first>
  <action>
Add `category: text('category'),` column to `casks` table in `src/db/schema.ts` after line 24 (after `install_365d`, before `github_stars`). Column is nullable — matches D-03 (populated during pipeline enrichment, initially NULL for uncategorized casks).

Create migration file `src/db/migrations/0002_add_category.sql` using the pattern from 0001_add_search_vector.sql. Comment block: "Migration: Add category column for discovery filtering / Phase 04: discovery-layer / category filtering / Applied: 2026-05-26 / Note: Category populated during sync pipeline enrichment via AWS Bedrock ML categorization. Nullable during initial rollout; uncategorized casks remain NULL."

Migration SQL:
- `ALTER TABLE "casks" ADD COLUMN "category" text;`
- `CREATE INDEX "idx_casks_category" ON "casks" ("category");`

After creating the migration file, run: `npx drizzle-kit push` to apply the schema change to the database. This is a blocking operation — must complete before any code references the new column.
  </action>
  <verify>
    <automated>grep -c "category.*text" src/db/schema.ts</automated>
    <automated>test -f src/db/migrations/0002_add_category.sql</automated>
    <automated>grep -c "ALTER TABLE.*ADD COLUMN.*category" src/db/migrations/0002_add_category.sql</automated>
    <automated>grep -c "CREATE INDEX.*idx_casks_category" src/db/migrations/0002_add_category.sql</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts contains `category: text('category'),` between install_365d and github_stars
    - src/db/migrations/0002_add_category.sql exists with comment block referencing Phase 04
    - Migration file contains `ALTER TABLE "casks" ADD COLUMN "category" text;`
    - Migration file contains `CREATE INDEX "idx_casks_category" ON "casks" ("category");`
    - `npx drizzle-kit push` completes successfully (exit 0)
  </acceptance_criteria>
  <done>
Schema has category column (nullable text), migration file exists and has been applied to the database, index created for filtering performance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add query functions for filtered browsing</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts (current query patterns — unstable_cache, conditional WHERE, tags)
    - src/db/schema.ts (to reference new category column)
    - .planning/phases/04-discovery-layer/04-PATTERNS.md (lines 89-206 for exact query patterns)
    - .planning/phases/04-discovery-layer/04-RESEARCH.md (lines 226-265 for dynamic Drizzle patterns)
  </read_first>
  <action>
Add `asc` to the drizzle-orm imports on line 4: `import { and, desc, asc, eq, sql } from 'drizzle-orm';`

Add three new query functions to `src/lib/queries.ts` after the `searchCasks` function:

**1. getCasksPageFiltered** (lines 144-172 in PATTERNS.md):
- Takes `opts: { category?: string; sort: 'popular' | 'alphabetical' | 'updated'; page: number }`
- Builds conditions array starting with `eq(casks.is_active, true)`
- If `category` is defined (not null/undefined), push `eq(casks.category, category)` to conditions
- Dynamic orderClause: `sort === 'alphabetical' ? asc(casks.name) : sort === 'updated' ? desc(casks.last_synced_at) : desc(casks.install_365d)`
- Query: `.where(and(...conditions)).orderBy(orderClause).limit(PAGE_SIZE).offset(offset)`
- Wrapped in `unstable_cache` with key `['casks-filtered']` and `{ tags: ['casks'] }`

**2. getCasksCountFiltered** (lines 176-191 in PATTERNS.md):
- Takes `category?: string`
- Same conditional WHERE pattern (only add category filter if defined)
- Returns `count(*)::int` aggregation
- Wrapped in `unstable_cache` with key `['casks-count-filtered']` and `{ tags: ['casks'] }`

**3. getCategories** (lines 195-206 in PATTERNS.md):
- No parameters
- Returns `.selectDistinct({ category: casks.category }).from(casks).where(and(eq(casks.is_active, true), sql`${casks.category} IS NOT NULL`)).orderBy(asc(casks.category))`
- Wrapped in `unstable_cache` with key `['categories']` and `{ tags: ['casks'] }`

All three functions use the same ISR cache pattern as existing queries (tags: ['casks']).
  </action>
  <verify>
    <automated>grep -c "import.*asc" src/lib/queries.ts</automated>
    <automated>grep -c "getCasksPageFiltered" src/lib/queries.ts</automated>
    <automated>grep -c "getCasksCountFiltered" src/lib/queries.ts</automated>
    <automated>grep -c "getCategories" src/lib/queries.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/queries.ts imports `asc` from drizzle-orm
    - Function `getCasksPageFiltered` exists with signature matching `opts: { category?: string; sort: string; page: number }`
    - Function builds conditions array conditionally (only adds category filter if category defined)
    - Dynamic orderClause uses ternary for sort selection (alphabetical → asc(name), updated → desc(last_synced_at), default → desc(install_365d))
    - Function `getCasksCountFiltered` exists with optional category parameter
    - Function `getCategories` exists with selectDistinct and IS NOT NULL filter
    - All three functions wrapped in unstable_cache with tags: ['casks']
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
Query layer has three new functions: getCasksPageFiltered (with conditional category filter and dynamic sorting), getCasksCountFiltered (for pagination with filters), and getCategories (for populating filter UI). All follow existing ISR cache patterns.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (URL params) | Untrusted category and sort params from ?category=X&sort=Y URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | src/lib/queries.ts getCasksPageFiltered | mitigate | Validate sort param against whitelist ['popular', 'alphabetical', 'updated'] before query construction; reject invalid values with fallback to 'popular' |
| T-04-02 | Tampering | src/lib/queries.ts getCasksPageFiltered | mitigate | Category param validated via Drizzle parameterized query (eq(casks.category, ?)) — prevents SQL injection |
| T-04-03 | Information Disclosure | src/lib/queries.ts getCategories | accept | Exposes list of categories (public information); no PII or sensitive data |
</threat_model>

<verification>
## Schema Verification
```bash
grep "category.*text" src/db/schema.ts
```
Expected: `category: text('category'),` present in casks table definition.

## Migration Applied
```bash
npx drizzle-kit push
```
Expected: Exit 0, "No schema changes detected" if already applied.

## Query Functions Exist
```bash
grep -E "export (const|async function) get(CasksPageFiltered|CasksCountFiltered|Categories)" src/lib/queries.ts
```
Expected: Three export declarations found.

## TypeScript Clean
```bash
npx tsc --noEmit
```
Expected: Exit 0, no type errors.
</verification>

<success_criteria>
- [x] Category column added to casks table schema (nullable text)
- [x] Migration file created and applied with category column and index
- [x] getCasksPageFiltered function handles optional category filter and dynamic sort order
- [x] getCasksCountFiltered function returns count with optional category filter for pagination
- [x] getCategories function returns distinct category list for filter UI population
- [x] All queries use unstable_cache with tags: ['casks'] for ISR compatibility
- [x] TypeScript compiles with no errors
- [x] Drizzle migration applied successfully
</success_criteria>

<output>
Create `.planning/phases/04-discovery-layer/04-01-SUMMARY.md` when done
</output>
