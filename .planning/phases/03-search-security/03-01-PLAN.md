---
phase: 03-search-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/queries.ts
  - src/db/migrations/
autonomous: true
requirements:
  - SRCH-01
user_setup: []

must_haves:
  truths:
    - "The casks table has a search_vector tsvector GENERATED ALWAYS AS STORED column"
    - "A GIN index named idx_casks_search_vector exists on the search_vector column"
    - "queries.ts exports a searchCasks(q: string) function using plainto_tsquery"
    - "queries.ts exports SEARCH_RESULT_CAP = 50"
    - "The migration SQL file exists in src/db/migrations/ and has been applied to the live database"
    - "The existing sync pipeline insert path is unaffected (search_vector excluded from $inferInsert)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "casks table schema with search_vector generated column and GIN index"
      contains: "generatedAlwaysAs"
    - path: "src/lib/queries.ts"
      provides: "searchCasks function and SEARCH_RESULT_CAP constant"
      exports: ["searchCasks", "SEARCH_RESULT_CAP"]
    - path: "src/db/migrations/"
      provides: "versioned SQL migration file containing ALTER TABLE + CREATE INDEX"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/lib/queries.ts"
      via: "CaskSelectRow type used as searchCasks return type"
      pattern: "CaskSelectRow"
    - from: "src/db/migrations/*.sql"
      to: "Neon Postgres"
      via: "drizzle-kit migrate"
      pattern: "search_vector"
---

<objective>
Add the tsvector generated column and GIN index to the casks table, then add the searchCasks query function to the queries layer. This plan delivers the complete database and query foundation for SRCH-01 full-text search.

Purpose: Without the tsvector column in Postgres and a query function to call it, the search API route (Plan 02) cannot function. This is the data layer that Plan 02 builds on.
Output: Updated schema.ts, migrated Neon Postgres database (search_vector column + GIN index live), updated queries.ts with searchCasks and SEARCH_RESULT_CAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-search-security/03-CONTEXT.md
@.planning/phases/03-search-security/03-RESEARCH.md
@.planning/phases/03-search-security/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tsvector column + GIN index to schema.ts</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts — current table definition; all existing columns must be preserved unchanged
    - .planning/phases/03-search-security/03-PATTERNS.md §Pattern assignments / schema.ts — exact imports and column declaration pattern to follow
    - .planning/phases/03-search-security/03-RESEARCH.md §Pattern 1 — Drizzle tsvector Generated Column
  </read_first>
  <action>
    Modify src/db/schema.ts to add the tsvector custom type and search_vector generated column. Per D-06 and D-07.

    Extend the import from 'drizzle-orm/pg-core' to also import `customType` and `index`. Add a second import line for `SQL` and `sql` from 'drizzle-orm'.

    Declare the tsVector custom type ABOVE the pgTable call:
      const tsVector = customType with dataType() returning 'tsvector'.

    Change the pgTable call signature from the two-argument form (tableName, columns) to the three-argument form (tableName, columns, tableConfig). The tableConfig callback receives `(t)` and returns an array containing one entry: `index('idx_casks_search_vector').using('gin', t.search_vector)`.

    Add the search_vector column as the last property of the columns object:
      search_vector: tsVector('search_vector').generatedAlwaysAs((): SQL => sql`to_tsvector('english', coalesce(${casks.name}, '') || ' ' || coalesce(${casks.description}, ''))`)

    All existing columns (id through last_synced_at) remain exactly as they are. The type exports CaskInsertRow and CaskSelectRow at lines 25-26 remain unchanged — Drizzle automatically excludes generated columns from $inferInsert.

    Critical: the generatedAlwaysAs callback references casks.name and casks.description by column object reference, not by string. The self-reference works because Drizzle uses lazy evaluation for the sql tag in generatedAlwaysAs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts imports customType and index from 'drizzle-orm/pg-core'
    - src/db/schema.ts imports SQL and sql from 'drizzle-orm'
    - src/db/schema.ts contains the string "tsVector" declared as a customType
    - src/db/schema.ts contains "generatedAlwaysAs" on the search_vector column
    - src/db/schema.ts contains "idx_casks_search_vector" in the table index declaration
    - src/db/schema.ts contains ".using('gin'" for the GIN index type
    - npx tsc --noEmit exits with code 0 (no TypeScript errors)
    - All 17 original columns (id through last_synced_at) remain present and unchanged
  </acceptance_criteria>
  <done>schema.ts compiles cleanly and declares the search_vector generated column with GIN index</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Run drizzle-kit generate + migrate to apply schema to Neon</name>
  <files>src/db/migrations/</files>
  <read_first>
    - src/db/schema.ts — must be the updated version from Task 1 (verify search_vector is present before running)
    - .planning/phases/03-search-security/03-RESEARCH.md §Standard Stack / drizzle-kit — migration approach
    - drizzle.config.ts — verify out directory and dbCredentials configuration
  </read_first>
  <action>
    Run the Drizzle migration in two steps. Per RESEARCH.md recommendation to use generate+migrate for production (produces auditable .sql file).

    Step 1: npx drizzle-kit generate
      This produces a versioned .sql file in src/db/migrations/ containing:
        ALTER TABLE "casks" ADD COLUMN "search_vector" tsvector GENERATED ALWAYS AS (to_tsvector('english', coalesce("name",'') || ' ' || coalesce("description",''))) STORED;
        CREATE INDEX "idx_casks_search_vector" ON "casks" USING gin ("search_vector");

    Step 2: npx drizzle-kit migrate
      This applies the generated SQL to the live Neon Postgres database. Requires DATABASE_URL to be set in environment (check .env.local). The migration will populate the search_vector column for all existing rows synchronously — for ~7,659 rows this takes seconds.

    If drizzle-kit migrate fails with a connection error, check that .env.local contains DATABASE_URL. If it fails with "column already exists", the migration was already applied — verify via Neon console or psql.

    Do NOT use drizzle-kit push — use generate + migrate to produce the auditable .sql file.
  </action>
  <verify>
    <automated>ls src/db/migrations/*.sql 2>/dev/null | head -5 && npx drizzle-kit migrate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - At least one .sql file exists in src/db/migrations/ containing the string "search_vector"
    - The .sql file contains "GENERATED ALWAYS AS" and "tsvector"
    - The .sql file contains "CREATE INDEX" and "idx_casks_search_vector"
    - drizzle-kit migrate exits without error (or reports "No pending migrations" if already applied)
    - After migration, running `psql $DATABASE_URL -c "\d casks"` (or equivalent Neon console query) shows search_vector column and idx_casks_search_vector index
  </acceptance_criteria>
  <done>Migration SQL file committed and applied; search_vector column and GIN index live in Neon Postgres</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add searchCasks function and SEARCH_RESULT_CAP to queries.ts</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts — current file; the getCasksPage and getCaskByToken patterns to follow; confirm existing imports (and, desc, eq, sql already imported on line 4)
    - .planning/phases/03-search-security/03-PATTERNS.md §queries.ts pattern — exact function signature and placement
    - src/db/schema.ts — confirm CaskSelectRow type export (used as return type annotation)
  </read_first>
  <action>
    Add two new exports to src/lib/queries.ts after the existing getTop500Tokens export. No import changes are needed — and, desc, eq, sql are already imported on line 4.

    First export: the SEARCH_RESULT_CAP constant
      export const SEARCH_RESULT_CAP = 50;
      Add a JSDoc comment: "Result cap for full-text search — single source of truth."

    Second export: the searchCasks async function
      Signature: export async function searchCasks(q: string): Promise<CaskSelectRow[]>
      Body: db.select().from(casks).where(and(eq(casks.is_active, true), sql`${casks.search_vector} @@ plainto_tsquery('english', ${q})`)).orderBy(sql`ts_rank(${casks.search_vector}, plainto_tsquery('english', ${q})) DESC`, desc(casks.install_365d)).limit(SEARCH_RESULT_CAP)
      Add a JSDoc comment: "Full-text search over cask name + description using tsvector/GIN index. NOT cached — search results must be fresh per query."

    Critical: do NOT wrap searchCasks in unstable_cache — search results must be fresh (unlike getCasksPage which uses ISR). Per RESEARCH.md anti-patterns.

    Critical: use a single .where(and(...)) call — do NOT chain .where() calls. The second .where() call overwrites the first in Drizzle. Per PATTERNS.md shared patterns.

    The CaskSelectRow import is already available via the existing `import { casks } from '@/db/schema'` — CaskSelectRow is re-exported from schema.ts and available via the same module.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/queries.ts exports SEARCH_RESULT_CAP with value 50
    - src/lib/queries.ts exports searchCasks as an async function
    - searchCasks uses and(eq(casks.is_active, true), sql`...@@ plainto_tsquery...`) in a single .where() call
    - searchCasks uses ts_rank in the orderBy clause
    - searchCasks uses desc(casks.install_365d) as a secondary sort
    - searchCasks uses .limit(SEARCH_RESULT_CAP)
    - searchCasks is NOT wrapped in unstable_cache
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>queries.ts exports searchCasks and SEARCH_RESULT_CAP; TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → database | Drizzle parameterizes all values via sql tag; no string concatenation into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | searchCasks / plainto_tsquery | mitigate | q value is passed as a Drizzle sql tag parameter (${q} becomes $1 in prepared statement); plainto_tsquery further sanitizes the input by parsing natural language tokens — no raw string concatenation |
| T-03-02 | Tampering | search_vector generated column | accept | Generated column is Postgres-managed; application cannot INSERT/UPDATE it; Drizzle $inferInsert excludes it automatically |
| T-03-SC | Tampering | npm/drizzle-kit invocation | mitigate | No new packages installed this phase; existing drizzle-kit already verified clean in package legitimacy audit |
</threat_model>

<verification>
After all three tasks complete:

1. TypeScript compiles with no errors: `npx tsc --noEmit`
2. Migration file exists: `ls src/db/migrations/*.sql` shows at least one file containing "search_vector"
3. Live database has the column: connect to Neon and run `SELECT column_name FROM information_schema.columns WHERE table_name = 'casks' AND column_name = 'search_vector'` — should return one row
4. GIN index exists: `SELECT indexname FROM pg_indexes WHERE tablename = 'casks' AND indexname = 'idx_casks_search_vector'` — should return one row
5. queries.ts exports are correct: grep for "export const SEARCH_RESULT_CAP" and "export async function searchCasks"
</verification>

<success_criteria>
- search_vector tsvector GENERATED ALWAYS AS STORED column exists in the live Neon casks table
- GIN index idx_casks_search_vector is active
- queries.ts exports searchCasks(q: string): Promise&lt;CaskSelectRow[]&gt; using plainto_tsquery with ts_rank ordering, limit 50
- TypeScript compiles cleanly with no errors introduced by schema changes
- Existing sync pipeline (cask insert/upsert) continues to work — search_vector excluded from $inferInsert
</success_criteria>

<output>
Create `.planning/phases/03-search-security/03-01-SUMMARY.md` when done.
</output>
