---
phase: 03-search-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/browse/loading.tsx
  - src/app/cask/[token]/loading.tsx
autonomous: true
requirements:
  - SRCH-01
user_setup: []

must_haves:
  truths:
    - "Navigating to /browse shows an animate-pulse skeleton grid while the server renders"
    - "Navigating to a cask detail page shows an animate-pulse skeleton hero and stats while the server renders"
    - "The browse skeleton matches CaskCard layout (icon square 52x52, name line, description lines, pill strip)"
    - "The cask skeleton matches the detail page hero layout (icon 80x80, h1 block, install code block, three stat tiles)"
    - "No layout shift occurs between skeleton and real content (dimensions match)"
  artifacts:
    - path: "src/app/browse/loading.tsx"
      provides: "Browse page skeleton — 12 pulse cards in the browse grid layout"
      contains: "animate-pulse"
    - path: "src/app/cask/[token]/loading.tsx"
      provides: "Cask detail skeleton — back nav, hero, install block, stats row"
      contains: "animate-pulse"
  key_links:
    - from: "src/app/browse/loading.tsx"
      to: "src/app/browse/page.tsx"
      via: "Next.js App Router automatic Suspense wrapping of loading.tsx in same route segment"
      pattern: "BrowseLoading"
    - from: "src/app/cask/[token]/loading.tsx"
      to: "src/app/cask/[token]/page.tsx"
      via: "Next.js App Router automatic Suspense wrapping of loading.tsx in same route segment"
      pattern: "CaskLoading"
---

<objective>
Add loading.tsx skeleton files to /browse and /cask/[token] route segments. Next.js App Router wraps these in Suspense automatically, providing instant visual feedback during page transitions with no layout shift.

Purpose: Delivers D-11, D-12, and D-13 — skeleton loading states for both main routes. Independent of the search implementation (Plan 01/02) and can run in Wave 1 in parallel.
Output: src/app/browse/loading.tsx (new), src/app/cask/[token]/loading.tsx (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-search-security/03-CONTEXT.md
@.planning/phases/03-search-security/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/app/browse/loading.tsx skeleton grid</name>
  <files>src/app/browse/loading.tsx</files>
  <read_first>
    - src/app/browse/page.tsx — main wrapper style (maxWidth: '1280px', margin: '0 auto', padding: '24px 24px 32px') and grid class from CaskGrid
    - src/components/cask-card.tsx — card layout dimensions: flex gap-16px padding-20px borderRadius-14px, icon 52x52 marginTop 2px, text body flex column gap-8px, name ~16px, description ~13px x2, metadata strip pills ~18px
    - src/components/cask-grid.tsx — confirm grid classes: className="grid grid-cols-1 md:grid-cols-2 gap-3"
    - .planning/phases/03-search-security/03-PATTERNS.md §browse/loading.tsx pattern — full skeleton layout
  </read_first>
  <action>
    Create src/app/browse/loading.tsx as a new file. Per D-11, D-12. This is a Server Component — no 'use client' directive.

    Export default function BrowseLoading():
      Returns a main element with the same wrapper styles as browse/page.tsx: maxWidth: '1280px', margin: '0 auto', padding: '24px 24px 32px'.
      Inside: a div with className="grid grid-cols-1 md:grid-cols-2 gap-3" (matching CaskGrid's grid).
      Map Array.from({ length: 12 }) to render 12 skeleton cards. Use index as key.
      Each card: div with className="animate-pulse" and inline styles matching CaskCard card container: display: 'flex', gap: '16px', padding: '20px', background: 'var(--color-surface)', border: '1px solid var(--color-border)', borderRadius: '14px'.
      Inside each card:
        Icon skeleton: div with width: 52, height: 52, borderRadius: 10, background: 'var(--color-border)', flexShrink: 0, marginTop: 2.
        Text body: div with flex: 1, minWidth: 0, display: 'flex', flexDirection: 'column', gap: '8px'.
          Name line: div with height: 16, borderRadius: 4, background: 'var(--color-border)', width: '55%'.
          Description line 1: div with height: 13, borderRadius: 4, background: 'var(--color-border)', width: '90%'.
          Description line 2: div with height: 13, borderRadius: 4, background: 'var(--color-border)', width: '70%'.
          Metadata strip: div with display: 'flex', gap: 8, marginTop: 4, containing two pill-shaped divs: first height: 18, width: 72, borderRadius: 9999, background: 'var(--color-border)'; second height: 18, width: 52, borderRadius: 9999, background: 'var(--color-border)'.

    Use var(--color-border) as the skeleton fill color — matches existing surface contrast in dark theme.
    Use inline style={{}} for layout (not Tailwind classes) except for grid and animate-pulse — consistent with the existing codebase pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/browse/loading.tsx exists as a new file
    - File does NOT have 'use client' directive
    - File exports a default function (BrowseLoading)
    - File contains "animate-pulse" class on each skeleton card
    - File contains Array.from with length 12 for the skeleton count
    - Icon skeleton div has width: 52 and height: 52 matching CaskCard icon dimensions
    - Grid wrapper div has className="grid grid-cols-1 md:grid-cols-2 gap-3" matching CaskGrid
    - Main wrapper has maxWidth: '1280px' and padding: '24px 24px 32px' matching browse/page.tsx
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Browse loading.tsx renders 12 animate-pulse skeleton cards matching CaskCard layout with no layout shift</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/app/cask/[token]/loading.tsx skeleton detail page</name>
  <files>src/app/cask/[token]/loading.tsx</files>
  <read_first>
    - src/app/cask/[token]/page.tsx — read the first 80 lines: main wrapper style (maxWidth: '1100px', padding: '0 24px 48px'), back nav (paddingTop: '20px', height ~30px), hero section (padding: '40px 0 32px', flex gap-24px, icon 80x80 rounded-[18px], h1 fontSize: '2rem', description lines)
    - .planning/phases/03-search-security/03-PATTERNS.md §cask/[token]/loading.tsx pattern — full skeleton layout with measurements
  </read_first>
  <action>
    Create src/app/cask/[token]/loading.tsx as a new file. Per D-11, D-13. This is a Server Component — no 'use client' directive. Note: this file is co-located with page.tsx inside the [token] dynamic route segment.

    Export default function CaskLoading():
      Returns a main element with className="animate-pulse" and inline styles: maxWidth: '1100px', margin: '0 auto', padding: '0 24px 48px'.
      
      Back nav skeleton: div with paddingTop: '20px' containing a div with height: 30, width: 120, borderRadius: 6, background: 'var(--color-border)'.
      
      Hero skeleton: div with borderBottom: '1px solid var(--color-border)', padding: '40px 0 32px', display: 'flex', alignItems: 'flex-start', gap: '24px'. Contains:
        Icon skeleton: div with width: 80, height: 80, borderRadius: 18, background: 'var(--color-border)', flexShrink: 0.
        Hero body: div with display: 'flex', flexDirection: 'column', gap: '12px', flex: 1, paddingTop: '4px'. Contains:
          h1 skeleton: div height: 36, width: '40%', borderRadius: 6, background: 'var(--color-border)'.
          Version/date line: div height: 12, width: '30%', borderRadius: 4, background: 'var(--color-border)'.
          Description line 1: div height: 16, width: '80%', borderRadius: 4, background: 'var(--color-border)'.
          Description line 2: div height: 16, width: '65%', borderRadius: 4, background: 'var(--color-border)'.
      
      Install block skeleton: div with marginTop: '32px'. Contains:
        Label line: div height: 11, width: 60, borderRadius: 4, background: 'var(--color-border)', marginBottom: '10px'.
        Code block: div height: 48, maxWidth: 640, borderRadius: 10, background: 'var(--color-border)'.
      
      Stats row skeleton: div with display: 'flex', gap: '12px', flexWrap: 'wrap', marginTop: '32px'. Map ['30d', '90d', '365d'] to three stat tile divs each with background: 'var(--color-surface)', border: '1px solid var(--color-border)', borderRadius: 10, padding: '16px 20px', minWidth: 110, display: 'flex', flexDirection: 'column', gap: 8. Each tile contains two divs: one height: 10, width: 40 (label) and one height: 18, width: 60 (number), both borderRadius: 4, background: 'var(--color-border)'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/cask/[token]/loading.tsx exists as a new file
    - File is at the correct path: src/app/cask/[token]/loading.tsx (co-located with page.tsx)
    - File does NOT have 'use client' directive
    - File exports a default function (CaskLoading)
    - main element has className="animate-pulse"
    - main element has maxWidth: '1100px' matching the cask page wrapper
    - File contains icon skeleton with width: 80 and height: 80 (matching the real icon dimensions)
    - File maps ['30d', '90d', '365d'] to render three stat tile skeletons
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Cask loading.tsx renders animate-pulse skeleton matching hero + install + stats layout with no layout shift on navigation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → loading.tsx | No user input; these are static Server Components rendered at route transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | loading.tsx components | accept | No user data, no sensitive state; skeleton shapes are purely visual with no dynamic content |
| T-03-SC | Tampering | npm installs | accept | No new packages installed this phase |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript compiles clean: `npx tsc --noEmit`
2. Next build succeeds: `npx next build`
3. Browse loading: navigate to /browse in dev mode — when server is slow (simulate with throttled network in DevTools) the skeleton grid appears before real content
4. Cask loading: navigate to any /cask/[token] URL — skeleton hero and stats appear before real content
5. No layout shift: the skeleton card dimensions match the real CaskCard visually (same borderRadius, padding, icon position)
6. No console errors on either loading page
</verification>

<success_criteria>
- Both loading.tsx files exist in the correct route segments
- Both use animate-pulse for shimmer animation via Tailwind
- Both match the real page layout dimensions (no layout shift on content arrival)
- next build passes with no errors from the new files
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/03-search-security/03-03-SUMMARY.md` when done.
</output>
