---
phase: 01-data-pipeline
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/lib/fetch-allowlist.ts
  - src/app/api/cron/sync/route.ts
  - src/lib/github.ts
  - vercel.json
autonomous: true
requirements: [SECU-04, DATA-03]
gap_closure: true
gaps_closed:
  - "SECU-04: RFC 1918 172.16.0.0/12 range absent from BLOCKED_CIDR_PREFIXES"
  - "Env validation: GITHUB_TOKEN and BLOB_READ_WRITE_TOKEN absent from startup check"
  - "github.ts: onSecondaryRateLimit has no retryCount cap — unbounded retry loop"
  - "CR-01: No per-icon try/catch in route.ts Promise.all — single failure kills full sync"
  - "CR-04: maxDuration=800 in route.ts with no Fluid Compute opt-in in vercel.json"
  - "WR-01/WR-03: GitHub and icon queries lack is_active=true filter — processes soft-deleted casks"

must_haves:
  truths:
    - "All RFC 1918 ranges (10.x, 127.x, 169.254.x, 192.168.x, 172.16.x–172.31.x, ::1) are blocked in BLOCKED_CIDR_PREFIXES"
    - "A redirect from any allowlisted host to a 172.16.x–172.31.x address throws SSRF_BLOCKED"
    - "All four env vars (DATABASE_URL, CRON_SECRET, GITHUB_TOKEN, BLOB_READ_WRITE_TOKEN) are validated at startup; missing any returns HTTP 500 with descriptive JSON"
    - "onSecondaryRateLimit in github.ts caps retries at retryCount < 2"
    - "A single icon fetch failure inside the Promise.all group is caught per-icon, logged, and the sync continues"
    - "vercel.json contains a functions block that opts /api/cron/sync into Fluid Compute with maxDuration: 800"
    - "GitHub enrichment DB query includes eq(casks.is_active, true) filter"
    - "Icon pipeline DB query includes eq(casks.is_active, true) filter"
  artifacts:
    - path: "src/lib/fetch-allowlist.ts"
      provides: "Complete RFC 1918 SSRF block including 172.16/12 range"
      contains: "172.16."
    - path: "src/app/api/cron/sync/route.ts"
      provides: "Env validation for all four required vars; per-icon try/catch; is_active filters"
      contains: "GITHUB_TOKEN"
    - path: "src/lib/github.ts"
      provides: "Capped onSecondaryRateLimit handler"
      contains: "retryCount < 2"
    - path: "vercel.json"
      provides: "Fluid Compute opt-in for cron route"
      contains: "functions"
  key_links:
    - from: "src/lib/fetch-allowlist.ts"
      to: "BLOCKED_CIDR_PREFIXES"
      via: "Array.startsWith check"
      pattern: "172\\.16\\."
    - from: "src/app/api/cron/sync/route.ts"
      to: "missing-env array"
      via: "Array.filter"
      pattern: "GITHUB_TOKEN.*BLOB_READ_WRITE_TOKEN|BLOB_READ_WRITE_TOKEN.*GITHUB_TOKEN"
---

<objective>
Close all six gaps found during Phase 1 verification. Two are SECU-04/DATA-03 blockers; four
are correctness/reliability issues that allow the sync to silently fail or process stale data.

Purpose: Bring Phase 1 into full compliance with SECU-04 (complete RFC 1918 SSRF protection)
and DATA-03 (reliable GitHub enrichment under token-missing or rate-limit conditions), and
harden the sync pipeline against partial failures and Vercel execution time limits.

Output: Four files modified. No new files created. All six VERIFICATION.md gaps resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete SSRF protection and harden GitHub retry cap</name>
  <files>src/lib/fetch-allowlist.ts, src/lib/github.ts</files>

  <read_first>
    - src/lib/fetch-allowlist.ts — current BLOCKED_CIDR_PREFIXES array (line 8); understand existing prefix-based check pattern before extending it
    - src/lib/github.ts — current onSecondaryRateLimit handler (lines 17-19); note that onRateLimit already uses retryCount < 2 correctly on line 15
  </read_first>

  <action>
    In src/lib/fetch-allowlist.ts, replace the BLOCKED_CIDR_PREFIXES array on line 8 with the
    full RFC 1918 + loopback + link-local set. The current array is:
      ['127.', '10.', '192.168.', '169.254.', '::1']
    Add all 16 entries for the 172.16.0.0/12 range: '172.16.' through '172.31.' (i.e.,
    '172.16.', '172.17.', '172.18.', '172.19.', '172.20.', '172.21.', '172.22.', '172.23.',
    '172.24.', '172.25.', '172.26.', '172.27.', '172.28.', '172.29.', '172.30.', '172.31.').
    The final array has 21 entries. The check logic on line 17 (Array.some + startsWith) does not
    change — only the array contents change.

    In src/lib/github.ts, update the onSecondaryRateLimit handler on lines 17-19. The current
    signature is (_retryAfter, _options, _octokit) with no retryCount parameter.
    Change the signature to accept retryCount as the fourth parameter (same position as
    onRateLimit already does on line 14), and change the body to: return retryCount < 2.
    This mirrors the existing onRateLimit pattern exactly. The handler must NOT be async and
    must NOT add any sleep/setTimeout — the throttling plugin handles retry-after timing.
  </action>

  <verify>
    <automated>grep -c "172\." /Users/yash/Personal/brewindex/src/lib/fetch-allowlist.ts</automated>
    <automated>grep "onSecondaryRateLimit" /Users/yash/Personal/brewindex/src/lib/github.ts</automated>
    <automated>npx tsc --noEmit --project /Users/yash/Personal/brewindex/tsconfig.json 2>&1 | grep "error TS" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - fetch-allowlist.ts BLOCKED_CIDR_PREFIXES contains exactly 21 entries (5 original + 16 new 172.xx. entries)
    - grep -c "172\." src/lib/fetch-allowlist.ts returns 16
    - The entries cover 172.16. through 172.31. with no gaps
    - github.ts onSecondaryRateLimit handler signature includes retryCount as fourth parameter
    - github.ts onSecondaryRateLimit body is: return retryCount < 2 (not: return true)
    - npx tsc --noEmit exits 0 (zero TypeScript errors)
  </acceptance_criteria>

  <done>
    fetch-allowlist.ts blocks all RFC 1918 private IP ranges. github.ts caps secondary rate
    limit retries at 2, matching the onRateLimit cap already in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix env validation, icon reliability, Fluid Compute opt-in, and is_active filters</name>
  <files>src/app/api/cron/sync/route.ts, vercel.json</files>

  <read_first>
    - src/app/api/cron/sync/route.ts — full file; key lines: 21 (missing-env array), 74-77 (icon query), 84-98 (Promise.all icon loop), 103-106 (GitHub query)
    - vercel.json — current structure; note the single "crons" array with no "functions" block
  </read_first>

  <action>
    In src/app/api/cron/sync/route.ts, make four targeted changes:

    Change 1 — Env validation (line 21):
    Replace: const missing = ['DATABASE_URL', 'CRON_SECRET']
    With:    const missing = ['DATABASE_URL', 'CRON_SECRET', 'GITHUB_TOKEN', 'BLOB_READ_WRITE_TOKEN']
    Note: CRON_SECRET is already used as the auth guard on line 17, so its presence in this
    array is dead code (it can never trigger a 500), but removing it would change existing
    behavior — leave it in place and simply append the two new vars.

    Change 2 — Icon query is_active filter (lines 74-77):
    The current query is: .where(isNull(casks.icon_url))
    Change to:            .where(and(isNull(casks.icon_url), eq(casks.is_active, true)))
    Import 'and' from 'drizzle-orm' alongside the existing sql/notInArray/eq/isNull/like
    imports on line 3.

    Change 3 — Per-icon try/catch inside Promise.all (lines 86-97):
    The current map callback is:
      async (c) => { const { url, isFallback } = await fetchAndStoreIcon(...); await db.update...; ... }
    Wrap the entire body of the map callback in try/catch. On catch, log the error with
    console.warn('[cron/sync] icon failed for', c.token, err) and continue (do not rethrow).
    This prevents a single transient DuckDuckGo timeout or Vercel Blob quota error from
    aborting all remaining icons AND skipping GitHub enrichment AND suppressing revalidateTag.

    Change 4 — GitHub query is_active filter (lines 103-106):
    The current query is: .where(like(casks.homepage, '%github.com%'))
    Change to:            .where(and(like(casks.homepage, '%github.com%'), eq(casks.is_active, true)))
    The 'and' import added in Change 2 covers this.

    In vercel.json, add a "functions" block that opts the cron route into Fluid Compute with
    maxDuration 800. The resulting vercel.json must have both "crons" and "functions" keys at
    the top level. The functions block maps the route pattern "app/api/cron/sync/route" to
    { "maxDuration": 800 }. This is the Vercel Fluid Compute opt-in pattern required for
    durations beyond 300s on Pro plan serverless.
  </action>

  <verify>
    <automated>grep "GITHUB_TOKEN" /Users/yash/Personal/brewindex/src/app/api/cron/sync/route.ts</automated>
    <automated>grep "BLOB_READ_WRITE_TOKEN" /Users/yash/Personal/brewindex/src/app/api/cron/sync/route.ts</automated>
    <automated>grep "is_active" /Users/yash/Personal/brewindex/src/app/api/cron/sync/route.ts</automated>
    <automated>grep -c "console.warn" /Users/yash/Personal/brewindex/src/app/api/cron/sync/route.ts</automated>
    <automated>grep "functions" /Users/yash/Personal/brewindex/vercel.json</automated>
    <automated>npx tsc --noEmit --project /Users/yash/Personal/brewindex/tsconfig.json 2>&1 | grep "error TS" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - route.ts missing-env array on line 21 contains 'GITHUB_TOKEN' and 'BLOB_READ_WRITE_TOKEN'
    - route.ts icon query .where() uses and(isNull(casks.icon_url), eq(casks.is_active, true))
    - route.ts GitHub query .where() uses and(like(casks.homepage, '%github.com%'), eq(casks.is_active, true))
    - route.ts Promise.all map callback has a try/catch wrapping fetchAndStoreIcon + db.update; catch block calls console.warn with c.token and err
    - route.ts imports 'and' from 'drizzle-orm' on the existing import line
    - vercel.json contains a "functions" key with a sub-object keyed to "app/api/cron/sync/route" with maxDuration: 800
    - npx tsc --noEmit exits 0 (zero TypeScript errors)
    - grep -c "is_active" route.ts returns 2 (one for icon query, one for GitHub query)
  </acceptance_criteria>

  <done>
    All four env vars validated at startup. Sync continues past individual icon failures.
    Fluid Compute opt-in ensures maxDuration=800 is respected on Pro plan. Both DB queries
    skip soft-deleted casks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron route → external APIs | Outbound HTTP via safeFetch; any redirect target must be validated against BLOCKED_CIDR_PREFIXES |
| env vars → runtime | Missing GITHUB_TOKEN causes silent operational degradation; validation added to make failure loud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | fetch-allowlist.ts redirect check | mitigate | Add 172.16–172.31 prefixes to close RFC 1918 gap; redirect to any private IP now throws SSRF_BLOCKED |
| T-05-02 | Denial of Service | github.ts onSecondaryRateLimit | mitigate | Cap retries at retryCount < 2; prevents function budget exhaustion from unbounded retry loop |
| T-05-03 | Denial of Service | icon Promise.all in route.ts | mitigate | Per-icon try/catch ensures single icon failure cannot abort GitHub enrichment or ISR invalidation |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan — all fixes are surgical edits to existing code |
</threat_model>

<verification>
After both tasks complete, run the following to confirm all six VERIFICATION.md gaps are closed:

```
# Gap 1: 172.16/12 range present
grep -c "172\." src/lib/fetch-allowlist.ts
# Expected: 16

# Gap 2a: All four env vars in missing-env check
grep -E "GITHUB_TOKEN|BLOB_READ_WRITE_TOKEN" src/app/api/cron/sync/route.ts
# Expected: both names appear on the missing-env line

# Gap 2b: onSecondaryRateLimit capped
grep "onSecondaryRateLimit" src/lib/github.ts
# Expected: handler body contains retryCount < 2

# CR-01: per-icon try/catch present
grep -c "console.warn" src/app/api/cron/sync/route.ts
# Expected: >= 1 (the per-icon catch warning)

# CR-04: Fluid Compute opt-in
grep "maxDuration" vercel.json
# Expected: 800

# WR-01/WR-03: is_active filters
grep -c "is_active" src/app/api/cron/sync/route.ts
# Expected: 2

# TypeScript clean
npx tsc --noEmit 2>&1 | grep "error TS" | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- fetch-allowlist.ts BLOCKED_CIDR_PREFIXES has 21 entries covering all RFC 1918 + loopback + link-local ranges; VERIFICATION.md truth 6 passes
- route.ts missing-env check includes all four vars: DATABASE_URL, CRON_SECRET, GITHUB_TOKEN, BLOB_READ_WRITE_TOKEN; VERIFICATION.md truth 5 passes
- github.ts onSecondaryRateLimit caps at retryCount < 2; no unbounded retry loop
- Icon sync continues past individual per-icon failures (try/catch per icon in Promise.all)
- vercel.json functions block opts /api/cron/sync into Fluid Compute with maxDuration: 800
- Both DB queries (icon pipeline + GitHub enrichment) filter to is_active = true only
- npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/01-data-pipeline/01-05-SUMMARY.md` when done
</output>
